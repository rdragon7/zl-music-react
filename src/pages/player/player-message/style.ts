import styled from 'styled-components'

export const PlayerMessageWrapper = styled.div`
  position: fixed;
  left: 50%;
  transform: translateX(-50%);
  bottom: 60px;
  height: 32px;
  padding: 0 15px;
  text-align: center;
  line-height: 32px;
  background-color: rgba(0, 0, 0, 0.5);
  color: #fff;
`
